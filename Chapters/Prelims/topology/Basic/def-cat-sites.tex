\begin{definition}\label[definition]{ba5}
The category $\sites$ has as objects sites and the morphisms are continuous functors.
When no confusion can arise then we will use $\cat{C}$ to denote the whole site $\site{C}{T}$.
\end{definition}