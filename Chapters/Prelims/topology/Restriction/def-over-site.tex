\begin{definition}[Oversite]\label[definition]{a7b}
Let $Y = \site{C}{T}$ be a site.
Let $a\in \cat{C}$.
Define the site $\oversite{Y}{a}$ to be the category $\overcat{C}{a}$ with the induced topology $T_a$.
\end{definition}
