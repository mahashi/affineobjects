\begin{definition}[Oversite]\label[definition]{c9e}
Let $Y = \site{C}{T}$ be a site.
Let $a\in \cat{C}$.
Define the site $\oversite{Y}{a}$ to be 
the category $\overcat{C}{a}$ with the induced topology $\top{T}_a$.
We will denote it by just $\overcat{C}{a}$.
\end{definition}