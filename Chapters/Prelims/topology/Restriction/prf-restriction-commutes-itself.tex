\begin{lemma}\label[lemma]{ad9}
Let $\cat{C}$ be a category.
Let $a,b\in \cat{C}$.
Let $f:b\rightarrow a$.
Then $\overcat{C}{b} \iso \overcat{\overcat{C}{a}}{f}$ and
$u_a \circ u_f = u_b$.
\end{lemma}
\begin{proof}

\end{proof}
