
% Let $\module{F}$ be a quasi-coherent module on $\overschemes{\Spec{R}}$
% via the presentation
% \[\coproduct_{I} \restrict{U_i}{\sheaf{O}} \rightarrow \coproduct_J \restrict{U_i}{\sheaf{O}} 
% 	\rightarrow \restrict{U_i}{\module{F}} \rightarrow 0\]

% for some cover $\cover{U_i}{\Spec{R}}$.
% We know from lemma ? that $\module{F} = \stilde{\gblsect{F}}$.
% Let $M$ be a $R$ module.

% We will show that
% \[\stilde{\gblsect{F}} \xrightarrow{\counit} \module{F}\]
% and
% \[M \xrightarrow{\unit} \gblsect{\stilde{M}}\]
% \[m \mapsto \omega^2(m)\]
% are isomorphisms.

% Let $f \in \gblsect{O}$.
% Let $M = \gblsect{F}$.
% Note that 
% \[\counit_{D(f)}: \Gamma(D(f); \stilde{M}) \rightarrow \Gamma(D(f);\module{F})\]
% is the map
% \[M \tensor \sect{D(f)}{O} \rightarrow M_f,\]
% \[m \tensor r \mapsto mr.\]

% This is an isomorphism by basic commutative algebra. Hence $\counit$ is an isomorphism, since domain and codomain are sheaves and $\counit$ is an isomorphism on the base of opens $D(f)$.

% By lemma ? in Stacks, $\gblsect{\stilde{M}} = M$.
% We have $\unit = \id_M$.