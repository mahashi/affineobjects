\begin{lemma}\label[lemma]{85e}
Let $X$ be a scheme.
Consider $k: \Open{X} \rightarrow \overschemes{X}$.
Then $\direct{K}\stilde = \stilde$
\end{lemma}
\begin{proof}
%TODO: proof
% 1. important components: terminal to terminal so domain of both stildes is the same.
% This implies that \direct{K}\pstilde = \pstilde
% 2. k is coverlifting and preserving
\end{proof}

%TODO: if sheaf of rings map k^# is an isomorphism
%and terminal maps to terminal -> restriction commutes with pstilde. 
\begin{lemma}\label[lemma]{86c}

\end{lemma}
