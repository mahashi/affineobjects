%TODO: State that it is not an ordinary adjunction but that both functors occur on the right side. 
%TODO: One cannot speak of a unit. Spell out the map out
\begin{lemma}\label[lemma]{85d}
The functor \[\Spec: \Rings \rightarrow \LocRingSpaces\]
is adjoint to \[\globalsections: \LocRingSpaces \rightarrow \Rings.\]
With canonical map 
\[\rspace{X}{O} \rightarrow \Spec{\globalsections{O}}.\]
\[ x \mapsto \ker(x),\]
\end{lemma}

\begin{proof}
%todo: ref
\end{proof}
