\begin{lemma}\label[lemma]{d66}
Let $\rsite{C}{T}{O}$ be a ringed site.
Let $f:b\rightarrow a \in \cat{C}$ and $g:c\rightarrow b \in \cat{C}$ be morphisms.
\begin{enumerate}
	\item If $fg$ and $f$ are restrictive, then $g$ is.
	\item if $f$ and $g$ are restrictive, then $fg$. 
\end{enumerate}

If $\sect{b}{O} \rightarrow \sect{c}{O}$ is faithfully flat
then $fg$ and $g$ restrictive implies $f$ is restrictive
\end{lemma}

\begin{proof}

Consider the following diagram
\begin{center}
	\begin{tikzcd}[row sep = large, column sep = large]
		\sect{a}{G} \tensor_{\sect{a}{O}} \sect{c}{O} \ar{r}{\widehat{f} \tensor_{\sect{b}{O}} \sect{c}{O}} \ar[equal]{d}
		& \sect{b}{G} \tensor_{\sect{b}{O}} \sect{c}{O} \ar{d}{\widehat{g}} \\
		\sect{a}{G} \tensor_{\sect{a}{O}} \sect{c}{O} \ar{r}{\widehat{fg}} 
		& \sect{c}{G},\\
	\end{tikzcd}
\end{center}

where $\widehat{\blank}$ is the adjunct map of $\sheaf{G}(\blank)$ with respect to the extension/restriction of scalars adjunction.

This diagram commutes: going either direction sends $g\tensor r$ to $rg$.
The results follows from commutativity.
\end{proof}

% Let $\sheaf{G}$ be a quasi-coherent sheaf on $\cat{C}$.
% We have the following identities.

% \begin{align*}
% \sheaf{G}(fg)\tensor_{\sect{a}{O}} \sect{c}{O} 
% 	&=(\sheaf{G}(g)(\sheaf{G}(f)\tensor_{\sect{a}{O}} \sect{b}{O}))\tensor_{\sect{b}{O}} \sect{c}{O}\\
% 	&= \sheaf{G}(g)\tensor_{\sect{b}{O}} \sect{c}{O} \circ \sheaf{G}(f)\tensor_{\sect{a}{O}} \sect{b}{O}\tensor_{\sect{b}{O}} \sect{c}{G}
% \end{align*}

% From this both results follow.


% \begin{remark}\label[remark]{d90}
% Knowing that $fg$ and $g$ are restrictive only implies that
% $$\sheaf{G}(f) \tensor_{\sect{a}{O}} \sect{b}{G} \tensor_{\sect{b}{O}} \sect{c}{G}$$
% is an isomorphism. 
% Only when $\sect{c}{O}$ is faithfully flat over $\sect{b}{O}$ would this imply that $f$ is restrictive.
% \end{remark}
